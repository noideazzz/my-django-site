version: '3.8'

services:
  web:
    build: .
    container_name: django_app
    restart: always
    ports:
      - "80:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./media:/app/media
    environment:
      - DJANGO_DEBUG=False
